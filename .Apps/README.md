# App Distribution Artifacts

This folder is the release drop location for mobile binaries.

- `.Apps/iOS`: store `.ipa` and App Clip builds.
- `.Apps/Android`: store `.apk` outputs.

Do not commit production signed binaries. Upload via CI release artifacts instead.
